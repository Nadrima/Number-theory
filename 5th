#include<bits/stdc++.h>
using namespace std;
void printBinary(int num){
   for(int i=10;i>=0;i--){
    cout<<((num>>i)&1);
   }
   cout<<endl;
}
int main()
{
    for(int c='A';c<='E';c++){
        printBinary(int(c));
    }
    for(int c='a';c<='e';c++){
        printBinary(int(c));
    }
    char A='a';
    char a=A&(~(1<<5));
    cout<<a<<endl;
    //'|' whith space(char(1<<5)==32 which represent space);
    cout<<char ('C'|(1<<5))<<endl;
}

